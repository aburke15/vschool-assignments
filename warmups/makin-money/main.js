//function formatMoney(amount) {
//    amount = amount.toFixed(2).replace(/(\d)(?=(\d{3})+\.)/g, "$1,"); 
//    return "$" + amount; 
//}

function formatMoney(amount) {
    // loop through the number adding a comma every three places starting after
    // index 2
    // conver to string 
    // split into array
}

console.log(formatMoney(1234567890));